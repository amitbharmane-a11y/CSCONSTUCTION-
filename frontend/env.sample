## API base URL
# For local development, point to your local FastAPI server:
# VITE_API_BASE_URL=http://localhost:8000

# In production on Vercel, leave VITE_API_BASE_URL **unset** so the
# frontend uses the relative "/api" path to talk to the same deployment.

